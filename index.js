// Code your solutions in this file
function writeCards(arr) {
  for (let i = 0; i < arr.length; i++) {
    arr[i] = `message for ${arr}.`
  }
}

function countdown(num) {
  for (let i = num; i >= 0; i--) {
    console.log(num)
  }
}